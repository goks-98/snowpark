name: snowflake-devops-demo

# Controls when the action will run. 
on:
  push:
    branches:
      - main
    paths:
      - 'database_change_scripts/**'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  deploy-snowflake-changes-job:
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use Python 3.8.x
        uses: actions/setup-python@v2.2.1
        with:
          python-version: 3.8.x

      - name: Run schemachange
        env:
          SF_ACCOUNT: ${{ secrets.SF_ACCOUNT }}
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SF_PASSWORD }}
          SF_USER_ROLE: ${{ secrets.SF_USER_ROLE }}
          SF_WAREHOUSE: ${{ secrets.SF_WAREHOUSE }}
          SF_DATABASE: ${{ secrets.SF_PROD_DATABASE }}
          SF_SCHEMA: ${{ secrets.SF_PROD_SCHEMA }}
          
          
        run: |
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          python --version
          echo "Step 1: Installing schemachange"
          pip install schemachange
          
          echo "Step 2: Running schemachange"
<<<<<<< HEAD
          schemachange -f $GITHUB_WORKSPACE/database_change_scripts -a $SF_ACCOUNT -u $SF_USERNAME -r $SF_USER_ROLE -w $SF_WAREHOUSE -d $SF_DATABASE -c $SF_DATABASE.SCHEMACHANGE.CHANGE_HISTORY --create-change-history-table --vars "{""target_db"": ""PROD_DB"", ""target_schema"": ""PROD_SCHEMA""}"
=======
          schemachange -f $GITHUB_WORKSPACE/database_change_scripts -a $SF_ACCOUNT -u $SF_USERNAME -r $SF_USER_ROLE -w $SF_WAREHOUSE -d $SF_DATABASE -c $SF_DATABASE.SCHEMACHANGE.CHANGE_HISTORY --create-change-history-table --vars '{target_db:PROD_DB, target_schema:PROD_SCHEMA}'
>>>>>>> 40be06fb81c52169f1a310a2a9751c73cfb860ff
